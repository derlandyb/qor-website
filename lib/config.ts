/**
 * Client-side mirror of `qor.polling.event_list_interval_seconds`
 * (api/config/qor.php) — same Tech Decision as qor-mobile's own mirror.
 * Keep this literal value in lockstep with the server default; there's no
 * endpoint that exposes config values to the client.
 */
export const EVENT_LIST_POLLING_INTERVAL_SECONDS = 60;
